[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Fallback SPA (react-router) — toutes les routes servent index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
